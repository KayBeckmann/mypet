name: mypet_backend
description: MyPet Backend API Server
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  # Web Framework
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_cors_headers: ^0.1.5

  # Datenbank
  postgres: ^3.1.1

  # Authentifizierung
  dart_jsonwebtoken: ^2.12.2
  bcrypt: ^1.1.3

  # Utilities
  dotenv: ^4.2.0
  uuid: ^4.2.2
  intl: ^0.19.0

  # Validierung
  validators: ^3.0.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.9
